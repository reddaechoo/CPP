#include <iostream>

// object: a region of storage(usually memory) that has a value and other associated properties
// variable: a named object
// identifier: the name of the object

int main()
{
	// Variable instantiation
	/*
	When the program is run(called runtime), the variable will be instantiated.
	Instantiation is a fancy word that means the object will be created 
	and assigned a memory address.
	Variables must be instantiated before they can be used to store values.
	*/

	int x; // define a variable named x, of type int



	// Data types
	// the type of a variable must be known at compile-time (when the program is compiled)



	//Defining multiple variables
	/*
	int a;
	int b;

	int a, b;  // int a, int b; // wrong (compiler error)

	int a, double b; // wrong (compiler error)

	int a; double b; // correct (but not recommended)

	// correct and recommended (easier to read)
	int a;
	double b;
	*/

	return 0;
}